import os

replacements = {
    'AuthUser': 'AuthUserModel', # In data layer
    'AuthUserEntity': 'AuthUser',
}

# This is a bit tricky with search and replace.
# I'll do it manually for the key files.
